export type ShortLinkData = {
  originalUrl?: string;
  shortenedUrl?: string;
  shortCode?: string;
};

export type ShortLinks = {
  originalUrl?: string;
  shortendedUrl?: string;
  shortCode?: string;
  id: string;
}[];
